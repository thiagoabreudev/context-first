# Context-First Platform - Environment Variables
# Copie este arquivo para .env e ajuste os valores conforme necessário

# =============================================================================
# BACKEND (FastAPI) - packages/backend/.env
# =============================================================================

# MongoDB (Local Docker)
MONGODB_URI=mongodb://context_first_app:app_password_change_in_production@localhost:27017/context_first_dev?authSource=context_first_dev
MONGODB_DB_NAME=context_first_dev

# MongoDB (Production - MongoDB Atlas)
# MONGODB_URI=mongodb+srv://user:password@cluster.mongodb.net/context_first_prod?retryWrites=true&w=majority
# MONGODB_DB_NAME=context_first_prod

# Redis (Local Docker)
REDIS_URL=redis://:dev_redis_password@localhost:6379/0

# Redis (Production - Upstash)
# REDIS_URL=redis://default:password@region.upstash.io:port
# REDIS_TOKEN=your_upstash_token

# Anthropic API (AI agents)
ANTHROPIC_API_KEY=sk-ant-... # Obter em https://console.anthropic.com

# AWS S3 (Checkpoints storage)
# TODO: Configurar quando necessário (IAD-8)
# AWS_ACCESS_KEY_ID=AKIA...
# AWS_SECRET_ACCESS_KEY=...
# AWS_S3_BUCKET=context-first-checkpoints
# AWS_REGION=us-east-1

# Clerk Authentication
# TODO: Configurar quando necessário (IAD-5)
# CLERK_SECRET_KEY=sk_live_...

# Application Settings
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# =============================================================================
# FRONTEND (Nuxt 3) - packages/frontend/.env
# =============================================================================

# API Base URL
API_BASE_URL=http://localhost:8000

# Clerk (Public Key)
# TODO: Configurar quando necessário (IAD-5)
# VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# WebSocket URL
VITE_WS_URL=ws://localhost:8000/ws

# =============================================================================
# DOCKER (Somente para referência - valores já em docker-compose.yml)
# =============================================================================

# MongoDB Admin
# MONGO_INITDB_ROOT_USERNAME=admin
# MONGO_INITDB_ROOT_PASSWORD=dev_password_change_in_production

# MongoDB App User (criado via mongo-init.js)
# MONGO_APP_USER=context_first_app
# MONGO_APP_PASSWORD=app_password_change_in_production

# Redis
# REDIS_PASSWORD=dev_redis_password

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================

# 1. NUNCA commite .env no git (já está no .gitignore)
# 2. Senhas padrão são APENAS para desenvolvimento local
# 3. Use senhas fortes em produção
# 4. Rotacione chaves API regularmente
# 5. MongoDB e Redis locais são para desenvolvimento - use Atlas/Upstash em produção
